<% if(quizes.length == 0 ){ %>
  No hay preguntas para su busqueda.
<%}else {%>
<table>
<% 
  var i;
  for (i=0; i< quizes.length; i++)
    { %>
    <tr><td><a href="quizes/<%= quizes[i].id %>"><%= quizes[i].pregunta %></a></td></tr>
<%
    }%>
  </table>
<%}%>
<a href="/quizes/new"><button type="button">Crear pregunta</button></a>